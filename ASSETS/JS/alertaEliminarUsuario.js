document.querySelectorAll(".btnEliminar").forEach(boton => {
    boton.addEventListener("click", () => {



        // poner la logica  lo cual seria que coja la variable del otro java script y mandarle ese dato
        // para que permita eliminar al usuaio, pero en si es desabilitar al usuario para que no ocurran 
       // errores en el codigo :3 
        
     });
})